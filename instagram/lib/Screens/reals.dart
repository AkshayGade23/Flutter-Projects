import 'package:flutter/material.dart';

class Reals extends StatelessWidget {
  const Reals({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Reals'));
  }
}
